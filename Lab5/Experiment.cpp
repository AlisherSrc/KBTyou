#include <iostream>
#include <algorithm>

using namespace std;

string yesorno(int find, int arr[]){
    
}

int main(){
    //int a[] = {3, 6, -1, 10};
    //sort(a, a+3);
    /*
    string s = "hello world and kbtu so kbtu is kbtu";
    string f = "kbtu";
    int tofind = s.find(f);
    while(tofind != string::npos){
        tofind = s.find(f, tofind + f.size());
        cout << " " << tofind << " ";
    } 
    */
   int n;
   cin >> n;
   int array[n];
   for(int i = 0;i < n;i++){
       cin >> array[i];
   }
    int x;
    cin >> x;

}